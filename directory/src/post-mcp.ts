// Generated from docs/mcp-measurement-post.md by scripts/render_post.py. Do not edit here.
// Edit the markdown and regenerate, so the page and the repo never
// disagree about a number.
export const MCP_POST_HTML = "<h1>We sent one handshake to every remote server in the MCP registry. Half answer. A quarter are locked, which is not the same as broken.</h1>\n<p class=\"lede\"><em>Measured 2026-09-06 against the official registry at registry.modelcontextprotocol.io. We walked all 930 pages of it: 92,908 entries, which resolve to 27,422 unique server names and 16,923 unique remote HTTP endpoints. We sent each of those endpoints one real MCP <code>initialize</code> request. Servers shipped only as a local package were not probed, because there is nothing to call. Any single server below can be re-checked with one curl. The aggregates cannot be, short of re-running the harvest, and the script that does it is in the repo.</em></p>\n<p>MCP is the protocol agents use to discover and call tools. Several registries list servers, and the official one is the largest. What none of them publishes is what happens when you call every server on the list.</p>\n<h2>The numbers, two ways</h2>\n<div class=\"scroll\"><table><thead><tr><th></th><th>By listing (16,923)</th><th>By unique host (11,823)</th></tr></thead><tbody><tr><td>Answered an MCP handshake</td><td>51.1%</td><td>50.0%</td></tr><tr><td>Locked behind a 401 or 403</td><td>24.0%</td><td>30.0%</td></tr><tr><td>Failed outright</td><td>24.9%</td><td>20.0%</td></tr></tbody></table></div>\n<p>Both are true and they answer different questions. The left counts registry entries, which is how the registry presents itself. The right counts hosts, because a handful of them carry enormous numbers of listings: <code>gateway.pipeworx.io</code> with 1,314 listings (answers-mcp); <code>api.mcp.ai</code> with 1,115 listings (answers-mcp); <code>api.m2mcent.com</code> with 276 listings (not-mcp); <code>server.smithery.ai</code> with 216 listings (not-found).</p>\n<p>We publish both so nobody has to wonder which one we would have picked.</p>\n<h2>Locked is not broken</h2>\n<p>4,068 listed endpoints, 24%, answer with a 401 or a 403. They are running. They want a credential we do not have.</p>\n<p>A checker that sends one request and treats \"no result\" as \"dead\" calls every one of them broken. That is 4,068 endpoints it is wrong about, and it is the single most common way to get this measurement wrong.</p>\n<p>We record those as <code>auth-required</code>. Not as answering, because they did not, and not as failing, because they did not do that either.</p>\n<h2>What actually failed</h2>\n<div class=\"scroll\"><table><thead><tr><th>Outcome</th><th>Count</th></tr></thead><tbody><tr><td>Unreachable: DNS, timeout, refused</td><td>1,802</td></tr><tr><td>Not found (404)</td><td>1,041</td></tr><tr><td>Answered with something that is not MCP</td><td>680</td></tr><tr><td>Answered, but the JSON-RPC body was an error</td><td>340</td></tr><tr><td>Server error (5xx)</td><td>218</td></tr><tr><td>Bad request (other 4xx)</td><td>132</td></tr></tbody></table></div>\n<h2>What the answering servers expose</h2>\n<p>Of the 8,642 that answered, we followed with <code>tools/list</code>. 8,404 returned a usable list. 238 answered the handshake and then returned nothing we could parse as a tool list, and we record that rather than guess.</p>\n<p>Across those 8,404 servers: <strong>130,518 tools</strong>, a median of 7 per server, and one server exposing 1,082.</p>\n<h2>Three things we got wrong, in order</h2>\n<p><strong>The sample.</strong> We first probed 400 servers and found 45.8% locked and 12.7% failing. That was the first 400 entries in the registry's default order, which is not a sample, it is the top of a list.</p>\n<p><strong>The crawl.</strong> So we ran it properly, or thought we did. Our harvester had a page limit set to 400 pages. The registry is 930 pages. It stopped at 9,109 of 16,923 endpoints with the cursor still live, returned no error, and we described the result as complete. It was 54% of the registry, and it carried the same bias as the sample we had just retracted, three orders of magnitude larger.</p>\n<p><strong>The published figures.</strong> On the strength of that we said 45% answer and 30% are locked. The real numbers are 51% and 24%. We had also emailed three companies those figures and had to write to all three.</p>\n<p>Each of these is in here rather than quietly edited out. The failure rate is the one number that barely moved between the truncated run and the complete one, 24.9% both times, which is the only reason we noticed how little the truncation had cost us on that particular row.</p>\n<h2>What we did not do</h2>\n<p>We sent one <code>initialize</code> and, on success, one <code>tools/list</code>. We did not call a tool. We did not authenticate. We did not retry a server that timed out, so some of the 1,802 unreachable were probably briefly unlucky. We did not probe package-only servers. A server that \"answers a handshake\" here returned a valid <code>initialize</code> result. That is all it means.</p>\n<h2>Check any of this yourself</h2>\n<div class=\"scroll\"><pre class=\"mono\">curl -s -X POST https://&lt;server&gt;/mcp \\\n  -H 'Content-Type: application/json' \\\n  -H 'Accept: application/json, text/event-stream' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"initialize\",\"params\":{\"protocolVersion\":\"2025-06-18\",\"capabilities\":{},\"clientInfo\":{\"name\":\"you\",\"version\":\"0\"}}}'</pre></div>\n<p>The whole thing: <code>scripts/harvest_mcp.py</code> in the repo, and the dated result set is <code>data/mcp-verified-v2.json</code>.</p>";
