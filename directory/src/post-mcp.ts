// Generated from docs/mcp-measurement-post.md by scripts/render_post.py
// Do not edit here. Edit the markdown and regenerate, so the post and the
// repo never disagree about a number.
export const MCP_POST_HTML = "<h1>We sent one handshake to every remote server in the MCP registry. 45% answer. 30% are locked, which is not the same as broken.</h1>\n<p>*Measured 2026-09-05 against the official registry at registry.modelcontextprotocol.io. It listed roughly 37,000 servers that day; 9,109 of them expose a remote HTTP endpoint, and we sent each one a real MCP <code>initialize</code> request. Servers that ship only as a local package were not probed, because there is nothing to call. Any single server below can be re-checked with one curl. The aggregates cannot be, short of re-running the harvest, and the script that does it is in the repo.*</p>\n<p>MCP is the protocol agents use to discover and call tools. There are several registries listing servers, and the official one is the largest. What none of them publish is what happens when you actually call every server on the list.</p>\n<p>We did that for x402 endpoints last month and learned two things: the network turns over faster than any directory can track, and our own first attempt was wrong in a way we had to retract. So we ran the same method here, and we are stating the numbers two ways, because the first way we stated them was flattering and we did not notice until we checked.</p>\n<h2>The numbers, two ways</h2>\n<div class=\"scroll\"><table><thead><tr><th></th><th>By listing (9,109)</th><th>By unique host (7,377)</th></tr></thead><tbody><tr><td>Answered an MCP handshake</td><td>44.8%</td><td>49.5%</td></tr><tr><td>Locked behind a 401 or 403</td><td>30.3%</td><td>32.9%</td></tr><tr><td>Failed outright</td><td>24.9%</td><td>17.6%</td></tr></tbody></table></div>\n<p>Both columns are true. The left one counts registry entries, which is how the registry presents itself. The right one counts hosts, because three of them account for most of the gap on the failing row: <code>api.m2mcent.com</code> has 276 entries and every one is a 404, one <code>trycloudflare.com</code> tunnel has 90 entries and none resolve, and <code>server.smithery.ai</code> proxies 213 entries of which most are 404. Counted once each, the failure rate is 17.6%, not 24.9%.</p>\n<p>We show both so nobody has to wonder which one we would have picked.</p>\n<h2>The headline: locked is not broken</h2>\n<p>2,762 listed servers, 30%, answer our handshake with a 401 or a 403. They are running. They want a credential we do not have. A checker that sends one request and treats \"did not get a result\" as \"dead\" calls every one of them broken, and is wrong about a third of the registry before it starts.</p>\n<p>This is the same shape as the finding on the x402 side, where a GET-only probe could not see the 52% of endpoints that only answer POST. In both cases the error is not in the servers. It is in assuming the probe's failure is the server's failure.</p>\n<p>We record those 2,762 as <code>auth-required</code>. Not as answering, because they did not, and not as failing, because they did not do that either.</p>\n<h2>What actually failed</h2>\n<div class=\"scroll\"><table><thead><tr><th>Outcome</th><th>Count</th></tr></thead><tbody><tr><td>Not found (404)</td><td>874</td></tr><tr><td>Unreachable: DNS, timeout, refused</td><td>833</td></tr><tr><td>Answered, but the JSON-RPC body was an error</td><td>223</td></tr><tr><td>Answered with something that is not MCP</td><td>211</td></tr><tr><td>Server error (5xx)</td><td>98</td></tr><tr><td>Bad request (other 4xx)</td><td>26</td></tr></tbody></table></div>\n<p>Three hosts are most of this table, and they are listed above. Strip them out and the registry looks considerably healthier than the raw count suggests.</p>\n<h2>What the servers that answered are serving</h2>\n<p>Of the 4,082 that answered, we followed up with <code>tools/list</code>. 3,967 returned a tool list; 115 answered the handshake but returned nothing usable when asked for tools, and we record that rather than guess at it.</p>\n<p>Across those 3,967 servers: <strong>52,538 tools</strong>, a median of 6 per server, and one server exposing 627.</p>\n<h2>Live servers the registry does not list</h2>\n<p>We also called the MCP servers of six companies whose names come up when people talk about agent tooling. Four of the six answer a handshake and are not in the official registry at all: Firecrawl, Tavily, Browserbase and Nansen. Only Exa and Apify are listed, and Apify is listed 100 times, once per query-string variant, all of them locked.</p>\n<p>We are not saying the registry is incomplete as a criticism. We are saying a directory built from it alone misses live servers from well-known operators, which is a property worth knowing before you build on it.</p>\n<h2>We got a number wrong before we published it</h2>\n<p>Three weeks ago we probed a sample of 400 servers and found 45.8% locked and 12.7% failing. We nearly used those figures. The full run says 30.3% and 24.9%. The sample was the first 400 entries in the registry's default order, and the registry's default order is not random.</p>\n<p>We are leaving this in for the same reason we left the x402 retraction in. A measurement project that hides its corrections is worth nothing, and a sample that happens to be the top of a list is not a sample.</p>\n<h2>What we did not do</h2>\n<p>We sent one <code>initialize</code> and, on success, one <code>tools/list</code>. We did not call a tool. We did not authenticate. We did not retry a server that timed out, so some of the 833 unreachable may have been briefly unlucky. We did not probe servers that ship only as a package, which is roughly one in eight of the registry. A server that \"answers a handshake\" here is one that returned a valid <code>initialize</code> result. That is all it means.</p>\n<h2>Check any of this yourself</h2>\n<p>Any single server:</p>\n<div class=\"scroll\"><pre class=\"mono\">curl -s -X POST https://&lt;server&gt;/mcp \\\n  -H 'Content-Type: application/json' \\\n  -H 'Accept: application/json, text/event-stream' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"initialize\",\"params\":{\"protocolVersion\":\"2025-06-18\",\"capabilities\":{},\"clientInfo\":{\"name\":\"you\",\"version\":\"0\"}}}'</pre></div>\n<p>The whole thing: <code>scripts/harvest_mcp.py</code> in the repo, and the dated result set is <code>data/mcp-verified-v1.json</code>.</p>";
